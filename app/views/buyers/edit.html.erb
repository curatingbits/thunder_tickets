<div class="max-w-2xl mx-auto space-y-6">
  <!-- Header -->
  <div>
    <%= link_to "← Back to Buyer", @buyer, class: "text-blue-400 hover:text-blue-300 text-sm mb-2 inline-block" %>
    <h1 class="text-2xl sm:text-3xl font-bold text-white">Edit Buyer</h1>
    <p class="text-gray-400 mt-2"><%= @buyer.name %></p>
  </div>

  <!-- Form -->
  <div class="card">
    <%= render "form", buyer: @buyer %>
  </div>
</div>
