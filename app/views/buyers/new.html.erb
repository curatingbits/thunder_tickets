<div class="max-w-2xl mx-auto space-y-6">
  <!-- Header -->
  <div>
    <%= link_to "← Back to Buyers", buyers_path, class: "text-blue-400 hover:text-blue-300 text-sm mb-2 inline-block" %>
    <h1 class="text-2xl sm:text-3xl font-bold text-white">New Buyer</h1>
    <p class="text-gray-400 mt-2">Add a new buyer to track their purchases</p>
  </div>

  <!-- Form -->
  <div class="card">
    <%= render "form", buyer: @buyer %>
  </div>
</div>
