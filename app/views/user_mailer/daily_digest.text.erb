MADR TICKETS - DAILY DIGEST
===============================

Good morning, <%= @user.name %>!

<% if @games.any? %>
Here's a look at your next <%= @games.count %> upcoming games:

<% @games.each_with_index do |game, index| %>
<%= index + 1 %>. Thunder vs <%= game.opponent.name %>
   <%= game.game_date.strftime("%A, %B %d, %Y") %>

   Status: <% if game.fully_sold? %>SOLD OUT<% elsif game.partially_sold? %>PARTIAL<% else %>AVAILABLE<% end %>
   Tickets: <%= game.tickets_sold_count %> / <%= game.season.num_seats %> sold

   View details: <%= game_url(game) %>

<% end %>
<% else %>
No upcoming games scheduled at this time.
<% end %>

---

This is an automated daily digest from MADR TICKETS.
Visit your dashboard: <%= root_url %>
