<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background-color: #0f172a;
      color: #e5e7eb;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 40px 20px;
    }
    .header {
      text-align: center;
      margin-bottom: 32px;
    }
    .logo {
      font-size: 24px;
      font-weight: bold;
      color: #60a5fa;
      margin-bottom: 8px;
    }
    .subtitle {
      font-size: 14px;
      color: #94a3b8;
    }
    .card {
      background-color: #1e293b;
      border-radius: 12px;
      padding: 32px;
      margin-bottom: 24px;
    }
    .greeting {
      font-size: 18px;
      color: #e5e7eb;
      margin-bottom: 16px;
    }
    .intro-text {
      color: #cbd5e1;
      line-height: 1.6;
      margin-bottom: 24px;
    }
    .games-section {
      margin-bottom: 24px;
    }
    .game-card {
      background-color: #334155;
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 16px;
      border-left: 4px solid #60a5fa;
    }
    .game-card:last-child {
      margin-bottom: 0;
    }
    .game-header {
      display: flex;
      justify-content: space-between;
      align-items: start;
      margin-bottom: 12px;
      flex-wrap: wrap;
    }
    .game-matchup {
      font-size: 18px;
      font-weight: 600;
      color: #ffffff;
      margin-bottom: 4px;
    }
    .game-date {
      font-size: 14px;
      color: #94a3b8;
      margin-bottom: 8px;
    }
    .status-badge {
      display: inline-block;
      padding: 4px 12px;
      border-radius: 12px;
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    .status-sold {
      background-color: rgba(239, 68, 68, 0.2);
      color: #fca5a5;
      border: 1px solid rgba(239, 68, 68, 0.4);
    }
    .status-partial {
      background-color: rgba(251, 191, 36, 0.2);
      color: #fcd34d;
      border: 1px solid rgba(251, 191, 36, 0.4);
    }
    .status-available {
      background-color: rgba(34, 197, 94, 0.2);
      color: #86efac;
      border: 1px solid rgba(34, 197, 94, 0.4);
    }
    .ticket-info {
      font-size: 14px;
      color: #cbd5e1;
      margin-top: 8px;
    }
    .view-button {
      display: inline-block;
      background-color: #2563eb;
      color: #ffffff;
      text-decoration: none;
      padding: 10px 20px;
      border-radius: 6px;
      font-weight: 600;
      font-size: 14px;
      margin-top: 12px;
      transition: background-color 0.2s;
    }
    .view-button:hover {
      background-color: #1d4ed8;
    }
    .no-games {
      text-align: center;
      padding: 32px;
      color: #94a3b8;
    }
    .no-games-icon {
      font-size: 48px;
      margin-bottom: 16px;
    }
    .footer {
      text-align: center;
      padding: 24px;
      color: #64748b;
      font-size: 12px;
      border-top: 1px solid #334155;
    }
    .footer-link {
      color: #60a5fa;
      text-decoration: none;
    }
    @media only screen and (max-width: 600px) {
      .container {
        padding: 20px 16px;
      }
      .card {
        padding: 24px 16px;
      }
      .game-header {
        flex-direction: column;
      }
      .status-badge {
        margin-top: 8px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <div class="logo">THUNDER TICKETS</div>
      <div class="subtitle">Your Daily Game Digest</div>
    </div>

    <!-- Main Card -->
    <div class="card">
      <div class="greeting">Good morning, <%= @user.name %>!</div>

      <% if @games.any? %>
        <p class="intro-text">Here's a look at your next <%= @games.count %> upcoming games:</p>

        <div class="games-section">
          <% @games.each do |game| %>
            <div class="game-card">
              <div class="game-matchup">
                Thunder vs <%= game.opponent.name %>
              </div>

              <div class="game-date">
                <%= game.game_date.strftime("%A, %B %d, %Y") %>
              </div>

              <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap;">
                <div>
                  <% if game.fully_sold? %>
                    <span class="status-badge status-sold">Sold Out</span>
                  <% elsif game.partially_sold? %>
                    <span class="status-badge status-partial">Partial</span>
                  <% else %>
                    <span class="status-badge status-available">Available</span>
                  <% end %>
                </div>

                <div class="ticket-info">
                  <%= game.tickets_sold_count %> / <%= game.season.num_seats %> tickets sold
                </div>
              </div>

              <a href="<%= game_url(game) %>" class="view-button">View Game Details</a>
            </div>
          <% end %>
        </div>
      <% else %>
        <div class="no-games">
          <div class="no-games-icon">📅</div>
          <p>No upcoming games scheduled at this time.</p>
        </div>
      <% end %>
    </div>

    <!-- Footer -->
    <div class="footer">
      <p>
        This is an automated daily digest from Thunder Tickets.<br>
        <a href="<%= root_url %>" class="footer-link">Visit Dashboard</a>
      </p>
    </div>
  </div>
</body>
</html>
