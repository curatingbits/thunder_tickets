<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "MADR TICKETS" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full bg-gray-950 text-gray-100">
    <% if logged_in? %>
      <!-- Navigation -->
      <nav class="bg-gray-900 border-b border-gray-800 sticky top-0 z-50" data-controller="mobile-menu">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-16">
            <div class="flex items-center">
              <!-- Logo -->
              <div class="flex-shrink-0 flex items-center">
                <%= link_to root_path, class: "text-xl sm:text-2xl font-bold" do %>
                  <span class="text-blue-400">MADR</span> <span class="text-orange-400">TICKETS</span>
                <% end %>
              </div>

              <!-- Desktop Navigation Links -->
              <div class="hidden md:ml-8 md:flex md:space-x-8">
                <%= link_to "Dashboard", root_path, class: "inline-flex items-center px-3 py-2 text-sm font-medium #{current_page?(root_path) ? 'text-blue-400 border-b-2 border-blue-400' : 'text-gray-300 hover:text-white'}" %>
                <%= link_to "Games", games_path, class: "inline-flex items-center px-3 py-2 text-sm font-medium #{current_page?(games_path) ? 'text-blue-400 border-b-2 border-blue-400' : 'text-gray-300 hover:text-white'}" %>
                <%= link_to "Buyers", buyers_path, class: "inline-flex items-center px-3 py-2 text-sm font-medium #{current_page?(buyers_path) ? 'text-blue-400 border-b-2 border-blue-400' : 'text-gray-300 hover:text-white'}" %>
                <%= link_to "Users", users_path, class: "inline-flex items-center px-3 py-2 text-sm font-medium #{current_page?(users_path) ? 'text-blue-400 border-b-2 border-blue-400' : 'text-gray-300 hover:text-white'}" %>
                <%= link_to "Settings", settings_path, class: "inline-flex items-center px-3 py-2 text-sm font-medium #{current_page?(settings_path) ? 'text-blue-400 border-b-2 border-blue-400' : 'text-gray-300 hover:text-white'}" %>
              </div>
            </div>

            <!-- Right side items - Desktop Only -->
            <div class="hidden md:flex items-center space-x-4">
              <% if current_season %>
                <span class="text-sm text-gray-400">Season: <span class="text-blue-400 font-semibold"><%= current_season.year %></span></span>
              <% end %>
              <span class="text-sm text-gray-400">Welcome, <span class="text-white"><%= current_user.name %></span></span>
              <%= button_to "Logout", logout_path, method: :delete, class: "btn-secondary text-xs" %>
            </div>

            <!-- Mobile menu button -->
            <div class="flex items-center md:hidden">
              <button type="button" data-action="click->mobile-menu#toggle" class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-white hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-blue-500">
                <span class="sr-only">Open main menu</span>
                <!-- Hamburger icon -->
                <svg class="block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                </svg>
              </button>
            </div>
          </div>
        </div>

        <!-- Mobile menu overlay -->
        <div data-mobile-menu-target="overlay" data-action="click->mobile-menu#close" class="fixed inset-0 bg-black bg-opacity-50 z-40 hidden md:hidden"></div>

        <!-- Mobile menu panel -->
        <div data-mobile-menu-target="menu" class="fixed top-0 right-0 bottom-0 w-64 bg-gray-900 transform translate-x-full transition-transform duration-300 ease-in-out z-50 md:hidden">
          <div class="flex flex-col h-full">
            <!-- Close button -->
            <div class="flex items-center justify-between p-4 border-b border-gray-800">
              <span class="text-lg font-bold">
                <span class="text-blue-400">MADR</span> <span class="text-orange-400">TICKETS</span>
              </span>
              <button type="button" data-action="click->mobile-menu#close" class="p-2 rounded-md text-gray-400 hover:text-white hover:bg-gray-800">
                <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                </svg>
              </button>
            </div>

            <!-- User info -->
            <div class="p-4 border-b border-gray-800">
              <p class="text-sm text-gray-400">Welcome,</p>
              <p class="text-white font-semibold"><%= current_user.name %></p>
              <% if current_season %>
                <p class="text-xs text-gray-500 mt-2">Season: <span class="text-blue-400"><%= current_season.year %></span></p>
              <% end %>
            </div>

            <!-- Navigation links -->
            <nav class="flex-1 px-2 py-4 space-y-1">
              <%= link_to root_path, data: { action: "click->mobile-menu#closeMenu" }, class: "flex items-center px-3 py-3 text-base font-medium rounded-lg #{current_page?(root_path) ? 'bg-gray-800 text-blue-400' : 'text-gray-300 hover:bg-gray-800 hover:text-white'}" do %>
                <svg class="mr-3 h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
                </svg>
                Dashboard
              <% end %>
              <%= link_to games_path, data: { action: "click->mobile-menu#closeMenu" }, class: "flex items-center px-3 py-3 text-base font-medium rounded-lg #{current_page?(games_path) ? 'bg-gray-800 text-blue-400' : 'text-gray-300 hover:bg-gray-800 hover:text-white'}" do %>
                <svg class="mr-3 h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
                </svg>
                Games
              <% end %>
              <%= link_to buyers_path, data: { action: "click->mobile-menu#closeMenu" }, class: "flex items-center px-3 py-3 text-base font-medium rounded-lg #{current_page?(buyers_path) ? 'bg-gray-800 text-blue-400' : 'text-gray-300 hover:bg-gray-800 hover:text-white'}" do %>
                <svg class="mr-3 h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
                </svg>
                Buyers
              <% end %>
              <%= link_to users_path, data: { action: "click->mobile-menu#closeMenu" }, class: "flex items-center px-3 py-3 text-base font-medium rounded-lg #{current_page?(users_path) ? 'bg-gray-800 text-blue-400' : 'text-gray-300 hover:bg-gray-800 hover:text-white'}" do %>
                <svg class="mr-3 h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
                </svg>
                Users
              <% end %>
              <%= link_to settings_path, data: { action: "click->mobile-menu#closeMenu" }, class: "flex items-center px-3 py-3 text-base font-medium rounded-lg #{current_page?(settings_path) ? 'bg-gray-800 text-blue-400' : 'text-gray-300 hover:bg-gray-800 hover:text-white'}" do %>
                <svg class="mr-3 h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                </svg>
                Settings
              <% end %>
            </nav>

            <!-- Logout button -->
            <div class="p-4 border-t border-gray-800">
              <%= button_to logout_path, method: :delete, class: "w-full btn-danger flex items-center justify-center" do %>
                <svg class="mr-2 h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                </svg>
                Logout
              <% end %>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <!-- Flash Messages -->
    <% if flash.any? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <% flash.each do |type, message| %>
          <div class="rounded-lg p-4 mb-4 <%= type == 'notice' ? 'bg-green-900/50 border border-green-700 text-green-200' : 'bg-red-900/50 border border-red-700 text-red-200' %>">
            <div class="flex items-center">
              <span class="flex-1"><%= message %></span>
              <button onclick="this.parentElement.parentElement.remove()" class="text-gray-400 hover:text-white ml-4">×</button>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="<%= logged_in? ? 'max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8' : '' %>">
      <%= yield %>
    </main>
  </body>
</html>
